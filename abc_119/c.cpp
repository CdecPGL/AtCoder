// Score: /300

#include <iostream>
#include <vector>

using namespace std;

int main()
{
	int N, A, B, C;
	cin >> N >> A >> B >> C;
	vector<int> ls(N);
	for (int n = 0; n < N; ++n)
	{
		cin >> ls[n];
	}
	
}
